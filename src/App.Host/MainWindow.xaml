<mah:MetroWindow x:Class="App.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:App.Host"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:ui="clr-namespace:App.UI.Controls;assembly=App.UI"
        mc:Ignorable="d"
        Title="Script Writer" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New Script" Command="{Binding NewScriptCommand}"/>
                <MenuItem Header="_Open Script" Command="{Binding OpenScriptCommand}"/>
                <MenuItem Header="_Save" Command="{Binding SaveScriptCommand}"/>
                <MenuItem Header="Save _As" Command="{Binding SaveScriptAsCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Command="{x:Static ApplicationCommands.Close}"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" Command="{x:Static ApplicationCommands.Undo}"/>
                <MenuItem Header="_Redo" Command="{x:Static ApplicationCommands.Redo}"/>
                <Separator/>
                <MenuItem Header="Cu_t" Command="{x:Static ApplicationCommands.Cut}"/>
                <MenuItem Header="_Copy" Command="{x:Static ApplicationCommands.Copy}"/>
                <MenuItem Header="_Paste" Command="{x:Static ApplicationCommands.Paste}"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Script _Editor"/>
                <MenuItem Header="_Corkboard"/>
                <MenuItem Header="_Outline"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Margin="10">
            <ui:ScriptEditor x:Name="Editor" 
                            Background="White"
                            Foreground="Black"
                            ScrollViewer.VerticalScrollBarVisibility="Auto"
                            BorderThickness="1"
                            BorderBrush="LightGray"
                            IsEnabled="True"
                            Focusable="True"
                            AcceptsReturn="True"
                            AcceptsTab="True"
                            Margin="20"/>
        </Grid>
    </Grid>
</mah:MetroWindow>
