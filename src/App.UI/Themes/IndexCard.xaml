<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:App.UI.Controls">

    <Style TargetType="{x:Type controls:IndexCardControl}">
        <Setter Property="Width" Value="200"/>
        <Setter Property="Height" Value="150"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:IndexCardControl}">
                    <Grid>
                        <!-- Drop shadow effect -->
                        <Border CornerRadius="3" Margin="2,2,0,0">
                            <Border.Effect>
                                <DropShadowEffect Color="#666666" Direction="315" 
                                                BlurRadius="5" ShadowDepth="2" Opacity="0.5"/>
                            </Border.Effect>
                        </Border>
                        
                        <!-- Main card -->
                        <Border x:Name="MainBorder" 
                                Background="{TemplateBinding CardColor}"
                                BorderBrush="#CCCCCC"
                                BorderThickness="1"
                                CornerRadius="3">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Title -->
                                <TextBlock Text="{TemplateBinding Title}"
                                         FontWeight="Bold"
                                         TextWrapping="Wrap"
                                         Margin="0,0,0,5"/>

                                <!-- Summary -->
                                <TextBox Grid.Row="1"
                                         Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Summary, UpdateSourceTrigger=PropertyChanged}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         BorderThickness="0"
                                         Background="Transparent"/>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#666666"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderThickness" Value="2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>